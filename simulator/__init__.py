"""Recoup simulator — synthetic failed-mandate generator and hidden ground truth.

Nothing in `agent/` may import from this package (see report R: no-leakage).
`response.py` in particular holds the hidden response functions.
"""

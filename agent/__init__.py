"""Recoup agent — the decision side.

Nothing in this package may import `simulator/response.py` or any other hidden
ground-truth module: the agent operates only on the observable case record plus
its own config (report R3, the no-leakage boundary).
"""
